

let object_status = {

    'start': 0 ,
    'reset': 1,

}

//console.log(object_status['reset']);

function start () {

    if (object_status['reset']==0) {
    object_status['start']= 1;

    let rock = document.createElement('img');
    let paper = document.createElement('img');
    let sci = document.createElement('img');
    }
}